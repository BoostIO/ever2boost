require "spec_helper"

describe Ever2boost do
  it "has a version number" do
    expect(Ever2boost::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
